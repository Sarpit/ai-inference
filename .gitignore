# Runtime data directories (docker volume mounts)
litellm_data/
openwebui_data/
broker_data/

# TLS certificates — never commit private keys
certs/*.pem
certs/*.key
certs/*.crt
certs/*.p12

# SQLite databases (LiteLLM, broker)
*.db
*.sqlite3

# Environment variable overrides
.env
.env.*
!.env.example

# macOS
.DS_Store

# Editor
.vscode/
*.swp
